DATABASE_URL="file:./dev.db"
# Must be 32+ characters with reasonable entropy (lib/auth/session-config.ts enforces this).
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SESSION_PASSWORD=""
